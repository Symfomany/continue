import { useNavigate } from "react-router-dom";

function LoginPage() {
  const navigate = useNavigate();
  return (
    <div className="p-8">
      <h1>
      Page Login
      </h1>
    </div>
  );
}

export default LoginPage;
